fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       

# ── Task queue ─────────────────────────────────────────────
celery==5.4.0
redis==5.0.4                   # celery broker + cache

sqlalchemy==2.0.30
alembic==1.13.1                # migrations
asyncpg==0.29.0                # async postgres driver
psycopg2-binary==2.9.9         # sync driver (alembic needs this)
pgvector==0.2.5                # vector similarity search

#openai-whisper==20231117       # local whisper transcription
anthropic==0.26.1              # claude API
openai==1.30.1                 # embeddings (text-embedding-3-small)
torch==2.3.0                   # whisper dependency
torchaudio==2.3.0

ffmpeg-python==0.2.0           # ffmpeg python wrapper
pydub==0.25.1                  # audio utilities

boto3==1.34.110                #amazon sdk for cloudflare R2 storage

python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # password hashing

weasyprint==62.3

pydantic==2.7.1
pydantic-settings==2.2.1       # .env loading

httpx==0.27.0                  # async HTTP (for webhooks / alerts)